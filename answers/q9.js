function myFunction(num1, num2) {
  console.log(num3);
}

const num1 = 10;
const num2 = 1;
const num3 = 20;

myFunction(num3, num1);

// The function takes two arguments - num1 and num2.

// It will display a value on the console.

// Three variables (num1, num2, num3), have been declared with different numeric values.

// The function is called with two arguments - num3 and num1.

// I expect the console to display a value of 20, since the first argument called is num3, and so that's the first and only value that the console/function will accept.
