function myFunction(num, num1) {
  console.log(num1);
}

let num = 10;
let num1 = 2;

myFunction(num);

// The function takes two arguments (dataType = number).

// The variable 'num' holds a value of 10.

// The variable 'num1' holds a value of 2.

// The function is called once with the variable 'num'.

// I expect the function to return an error/undefined, as the 'num' variable is not defined inside the function.

// num1 will not be displayed as it has not been called by the function.
