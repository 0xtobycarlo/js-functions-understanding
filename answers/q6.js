function myFunction(num, num1) {
  console.log(num1);
}

let num = 10;
let num1 = 2;

myFunction(num1, num);

// The function takes two arguments (dataType = number).

// The console.log will only display one argument.

// Variable 'num' holds the value of 10.

// Variable 'num1' holds the value of 2.

// The function is called with two arguments.

// I would presume the console will show a value of 2, and an error that reads" num is not defined.
